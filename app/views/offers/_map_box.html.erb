<div class="container-wb">
  <div class="window-box-title">
    <h3><strong> <%= offer.company_name %></strong></h3>
  </div>

  <div class="Window-box-content">
    <p><strong>Address:</strong> <%= offer.address %></p>
    <p><strong>Prix: <%= offer.price %> €</strong></p>
    <p><%= offer.description.first(140) %>...</p>
  </div>

  <div id="button-box">
    <%= link_to "voir", offer_path(offer) %>
  </div>
</div>


